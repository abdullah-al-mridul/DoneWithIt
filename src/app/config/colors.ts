const colors: Colors = {
  primary: "#fc5c65",
  secondary: "#4ecdc4",
  black: "#000",
  white: "#fff",
};

interface Colors {
  primary: string;
  secondary: string;
  black: string;
  white: string;
}

export type { Colors };
export default colors;
